{
  "name": "lifeflow-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI Life OS — capture-to-routine engine (MVP core)",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
