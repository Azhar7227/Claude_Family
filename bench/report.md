# LifeFlow AI — Provider Benchmark Report

Dataset: **108** messy routine descriptions · 1 run each · generated by `npm run bench`.

| Provider (model) | Item F1 | Field acc | Recall | Precision | Valid. fails | ECE | Calib. gap | Tokens | Cost | Latency p50/p95 (ms) |
|---|---|---|---|---|---|---|---|---|---|---|
| stub (deterministic-stub-v1) | 95.6% | 71.7% | 94.4% | 96.7% | 0 | 0.277 | -0.28 | 0 | $0.0000 | 0/0 |

**Metric notes**
- *Item F1* — did it find the right items (by title keyword)?
- *Field acc* — of matched items, fraction with correct type/category/recurrence/time.
- *ECE* — expected calibration error (lower = stated confidence matches reality).
- *Calib. gap* — mean confidence − precision (positive = overconfident).
- *Cost* — estimated from token usage × editable price table in `src/ai/registry.ts`.

## Recommendation: **stub** (deterministic-stub-v1)

> Only the deterministic **stub** baseline ran — no provider API keys were configured. This proves the harness end-to-end and gives a floor to beat. Set `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY` and re-run to compare real models. The prior recommendation (see `docs/AI_PROVIDER_COMPARISON.md`) is **Gemini Flash** for the OCR/cost/latency profile.

_Report regenerated by `npm run bench`; do not hand-edit._
